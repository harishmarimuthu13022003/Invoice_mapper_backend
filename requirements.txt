# FastAPI and Server
fastapi
uvicorn

# MongoDB
motor
pymongo

# Pinecone (Vector Store)
pinecone-client

# LLM APIs - Direct HTTP calls (no langchain!)
requests

# TF-IDF Embeddings (no PyTorch!)
scikit-learn

# PDF Processing
pypdf

# Utilities
python-dotenv
pydantic
python-multipart